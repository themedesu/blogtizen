<header class="bg-dark py-4">
    <div class="container d-flex flex-wrap justify-content-center">
      <a href="{{ config('app.url') }}" class="d-flex align-items-center me-lg-auto text-dark text-decoration-none">
        <img src="{{ config('app.logo') }}" class="bi me-3 rounded-pill" width="50" height="50" alt="{{ config('app.name') }}"/>
        <h1 class="fs-4 fw-bold text-white mb-0">{{ config('app.name') }}</h1>
      </a>
    </div>
</header>